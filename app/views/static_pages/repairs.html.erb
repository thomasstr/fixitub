<ul class="media-list">
	<li class="media">
		<%= image_tag "iphone4collection.jpg", :class => "pull-left" %>
		<div class="media-body">
			<h2 class="media-heading">
				iPhone 4 Reperasjoner
			</h2>
			<p>Vi i FixIt UB vil tilby reparasjon av iPhoner. Med originale deler og skjermer med forskjellige farger, ønsker vi og gjøre det prisgunstig og hyggelig å få den reparert hos oss. </p>
		</div>
	</li>
	<hr>
</ul>
<br/>
<ul class="media-list">
	<% @products.each do |product| %>
	<li class="media">
		<%= image_tag product.avatar.url(:thumb), :class => "pull-left" %>
		<div class="media-body">
			<h4 class="media-heading">
				<%= link_to product.p_name, product_path(product) %>
			</h4>
			<%= product.p_about %>
			<div class="pull-right">
				<%= number_to_currency(product.p_price, :locale => :nb) %>
			</div>
			<div class="pull-right">
			    <% if product.stock == true %>
			    <%= image_tag "in_stock.gif" %>
			    <% else %>
			    <%= image_tag "not_in_stock.jpg" %>
			    <% end %>
			</div>
		</div>
	</li>
	<hr>
	<% end %>
</ul>

<!-- Button to trigger modal -->
<a href="#myModal" role="button" class="btn" data-toggle="modal">Launch demo modal</a>
 
<!-- Modal -->
<div id="myModal" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h3 id="myModalLabel">Modal header</h3>
  </div>
  <div class="modal-body">
	  <%= %>
  </div>
  <div class="modal-footer">
    <button class="btn" data-dismiss="modal" aria-hidden="true">Close</button>
    <button class="btn btn-primary">Save changes</button>
  </div>
</div>