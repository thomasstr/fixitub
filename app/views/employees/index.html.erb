<h1>Listing employees</h1>

<table class="table table-striped">
  <tr>
    <th>Name</th>
    <th>Email</th>
    <th></th>
  </tr>

<% @employees.each do |employee| %>
  <tr>
    <td><%= employee.name %></td>
    <td><%= employee.email %></td>
    <td><%= link_to 'Show', employee, :class => "btn" %>
	<%= link_to 'Edit', edit_employee_path(employee), :class => "btn" %>
	<%= link_to 'Destroy', employee, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-danger" %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Ny ansatt', new_employee_path, :class => "btn btn-small" %>
